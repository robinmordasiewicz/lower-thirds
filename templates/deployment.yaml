apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "video.fullname" . }}
  labels:
    app: {{ include "video.name" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "video.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "video.name" . }}
    spec:
      containers:
        - name: video
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 8000
          resources:
            {{- toYaml .Values.resources | nindent 12 }}

